<Window x:Class="WPF_UI.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_UI"
        mc:Ignorable="d"
        Title="ConnectionWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="connectButton" Grid.Row="2" Content="Connect" Click="connectButton_Click"/>
        <Button x:Name="sendButton" Grid.Row="2" Grid.Column="1" Content="Send" Click="sendButton_Click"/>
        <Button x:Name="refreshButton" Grid.Row="0" Grid.Column="1" Content="Refresh" Click="refreshButton_Click"/>
        <ItemsControl x:Name="gamesList" Grid.Row="1" Grid.Column="1" IsEnabled="False"/>
        <ItemsControl x:Name="messagesList" Grid.Row="1" Grid.Column="2" IsEnabled="False"/>
    </Grid>
</Window>
