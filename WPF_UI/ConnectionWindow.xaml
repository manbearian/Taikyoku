<Window x:Class="WPF_UI.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_UI"
        xmlns:System="clr-namespace:System;assembly=System.Runtime"
        mc:Ignorable="d"
        Title="ConnectionWindow" Height="278" Width="800" SourceInitialized="Window_SourceInitialized" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="NewGameButton" Grid.Row="1" Grid.Column="1" Content="New Game" Click="NewGameButton_Click" />
        <Button x:Name="JoinGameButton" Grid.Row="1" Grid.Column="0" Content="Join Game" IsEnabled="False" Click="JoinGameButton_Click" />
        <ListBox x:Name="gamesList" Grid.Row="0" Grid.Column="0" SelectionChanged="gamesList_SelectionChanged" MouseLeftButtonDown="gamesList_MouseLeftButtonDown" IsEnabled="False" >
            <System:String>Connecting...</System:String>
        </ListBox>
    </Grid>
</Window>
