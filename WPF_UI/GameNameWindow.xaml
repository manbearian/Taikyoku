<Window x:Class="WPF_UI.GameNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_UI"
        mc:Ignorable="d"
        Title="New Game..." Height="100" Width="125" ResizeMode="NoResize" ShowInTaskbar="False" WindowStyle="SingleBorderWindow" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="GameNameTextBox" Grid.ColumnSpan="3" Grid.RowSpan="2" Height="Auto"/>
        <Button x:Name="OKButton" Grid.Row="1" Grid.Column="2" Content="Create" IsDefault="True" Margin="2,2,2,2" Click="OKButton_Click"/>
        <Button x:Name="CancelButton" Grid.Row="1" Grid.Column="1" Content="Cancel" IsCancel="True" Margin="2,2,2,2" Click="CancelButton_Click" />
    </Grid>
</Window>
